version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: tradingbot-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: tradingbot

volumes:
  mongo_data:
